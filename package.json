{
  "name": "mommyflow",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy .",
    "check": "node --check src/app.js && node --check sw.js && node --check functions/api/_shared.js && node --check functions/api/login.js && node --check functions/api/signup.js && node --check functions/api/me.js && node --check functions/api/logout.js && node --check functions/api/load.js && node --check functions/api/save.js && node --check functions/api/health.js"
  },
  "dependencies": {
    "mongodb": "^6.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}