{
  "name": "mommyflow",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "wrangler pages dev .",
    "dev:api": "node server/index.js",
    "deploy": "wrangler pages deploy .",
    "start": "node server/index.js",
    "check": "node --check src/config.js && node --check src/app.js && node --check sw.js && node --check server/index.js && node --check functions/api/_proxy.js && node --check functions/api/login.js && node --check functions/api/signup.js && node --check functions/api/me.js && node --check functions/api/logout.js && node --check functions/api/load.js && node --check functions/api/save.js && node --check functions/api/health.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.19.2",
    "mongodb": "^6.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
